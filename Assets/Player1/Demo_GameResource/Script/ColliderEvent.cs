using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ColliderEvent : MonoBehaviour {
   /* private CharacterController parent;
    void Start()
    {

        parent = this.transform.root.transform.GetComponent<CharacterController>();
    }
    private void OnTriggerEnter2D(Collider2D other)
    {
    
        if (parent.OnceAttack)
            return;

        other.GetComponent<WoodDollMgr>().SwordHitted();
        Debug.Log("hit::" + other.name);

        if (this.GetComponent<BoxCollider2D>().enabled)
        {
            parent.OnceAttack = true;
        }

    }*/
}
